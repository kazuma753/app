 <%= flash[:notice] %>
<h1>Books</h1>
<table>
  <thead>
    <tr>
      <div class=" nav-list">
      <div class="nav-list-item1"><th>Title</th></div>
      <div class="nav-list-item2"><th>Body</th></div>
      <th colspan="3"></th>
    </div>
    </tr>
  </thead>


  <tbody>
  <% @books.each do |book| %>
    <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>

  

    <td><%= link_to "Show","/books/#{book.id}" %></td>
    <td><%= link_to "Edit",edit_book_path(book) %></td>
     <td><%= link_to "Destroy",destroy_book_path(book), method: :delete, "data-confirm" => "Are you sure？" %></td>
   </tr>
  </tbody>
 <% end %>
</table>

<section class="new-book">
<h2>New book</h2>
  <%= form_for(@book) do |f| %>

<% if @book.errors.any? %>
<b><%= @book.errors.count %> errors prohibited this book from being saved:</b>
<ul>
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>

<% end %>
</ul>
<% end %>

<h4>title</h4>
<%= f.text_field :title %>
<h4>body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>


<% end %>
  </section>
  </body>
</html>



  