<%= flash[:notice] %>
<h1></h1>
<section = class="edit clearfix">
<h1>Editing Book</h1>

<%= form_for(@book) do |f| %>

<% if @book.errors.any? %>
<b><%= @book.errors.count %> errors prohibited this book from being saved:</b>
<ul>
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>
<% end %>

<% end %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
 
  <div class="edit-item"><%= f.submit 'Update Book' %></div>
  <div class="edit-item1"><%= link_to "Show",book_path(@book) %> | 
 <%= link_to "Back",books_path %></div>
<% end %>
</section>
